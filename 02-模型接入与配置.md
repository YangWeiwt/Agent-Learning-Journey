# 02｜模型接入与配置

这篇笔记记录应用接入大模型平台时必须理解的配置项与排障方法。示例以当前 `ai-agent-demo` 使用的阿里云百炼和 `qwen-plus` 为主，但 API Key、模型名、Base URL、超时等概念也适用于其他模型平台。

`Agent-Learning-Journey` 使用 Spring AI OpenAI 兼容客户端同时直连两个 Provider：DeepSeek 使用 `DEEPSEEK_API_KEY` 与 `deepseek-v4-flash`，MiniMax 使用 `MINIMAX_API_KEY` 与 `MiniMax-M2.7`。请求通过 `provider` 字段路由，两个 Provider 的 Chat Memory 相互隔离。下面的百炼内容保留为 Provider 对比和原项目案例。

## 模型接入的四个核心信息

应用接入远程模型平台，通常至少需要以下信息：

| 配置 | 作用 | 配错后的常见现象 |
|---|---|---|
| API Key | 标识调用方并完成鉴权 | 401、403、启动失败 |
| Model ID | 指定实际调用的模型 | model not found、能力不匹配 |
| Base URL | 指定请求发送到哪个平台及协议端点 | 404、401、连接失败、协议不兼容 |
| Timeout | 限制建立连接和等待响应的时间 | 请求过早失败或长时间占用资源 |

还要确认账号地域、业务空间、套餐、模型权限和额度。API Key 正确并不代表它一定有权调用任意模型。

## 参考项目 ai-agent-demo 的接入方式

`ai-agent-demo/pom.xml` 引入了：

```xml
<dependency>
    <groupId>com.alibaba.cloud.ai</groupId>
    <artifactId>spring-ai-alibaba-starter-dashscope</artifactId>
</dependency>
```

Spring Boot 根据配置创建 DashScope 模型客户端，再通过 Spring AI 的 `ChatModel` 抽象注入应用。当前 `application.yml` 中的核心结构是：

```yaml
spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-plus
```

调用关系可以表示为：

```text
业务代码
  → Spring AI ChatClient / ChatModel
  → Spring AI Alibaba DashScope Starter
  → 百炼模型服务
  → qwen-plus
```

当前项目还配置了本地 Ollama：

```yaml
spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: gemma3:1b
```

这里的 `localhost:11434` 只属于 Ollama，不是百炼的 Base URL。阅读多模型配置时必须先看配置前缀属于哪个 Provider。

## 获取百炼 API Key

按照百炼官方流程：

1. 登录百炼控制台的 API Key 管理页面。
2. 选择 Key 所属账号和业务空间。
3. 按需要配置模型访问范围和 IP 白名单。
4. 创建并立即安全保存 Key。
5. 在应用运行环境中配置 Key，不把真实值写入仓库。

业务空间会影响模型访问权限、项目隔离和成本统计。生产环境应为不同应用或环境使用独立 Key，避免开发、测试和生产共用同一个高权限凭证。

## API Key 的安全配置

### 方案一：环境变量

百炼官方常用环境变量名是：

```text
DASHSCOPE_API_KEY
```

当前终端临时设置：

```bash
export DASHSCOPE_API_KEY='<YOUR_API_KEY>'
```

Spring 配置只引用变量，不保存真实值：

```yaml
spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
```

环境变量配置后，已经打开的 IDE、终端或正在运行的 Java 进程通常不会自动获得新值，需要重新启动对应进程。生产服务应通过部署平台的 Secret、容器 Secret 或服务管理器的环境文件注入。

### 方案二：本地配置文件

项目已经启用 `local` Profile，并在 `.gitignore` 中忽略了：

```text
application-local.yml
```

因此也可以在下面的本地文件中保存开发机配置：

```yaml
# src/main/resources/application-local.yml
spring:
  ai:
    dashscope:
      api-key: '<YOUR_API_KEY>'
```

该方案只适合受控的本地开发环境。团队文档和示例文件应只提供占位符，不应包含可用密钥。

### `.gitignore` 的边界

`.gitignore` 只对尚未被 Git 跟踪的文件生效。如果文件已经提交，后来再加入 `.gitignore`，Git 仍会继续跟踪它。

安全处理顺序是：

1. 立即在模型平台轮换或吊销已暴露的 Key。
2. 把真实 Key 移到环境变量、Secret 管理系统或被忽略的本地配置。
3. 从被跟踪文件中删除真实值。
4. 确认 Git 暂存区、提交记录、日志和构建产物中不存在 Key。
5. 如果 Key 已进入远端历史，根据团队流程清理历史并通知相关成员重新同步。

只把 Key 从最新文件中删除，不能让已经泄露的 Key 重新变安全；轮换 Key 才是第一优先级。

## 选择模型

模型名在 API 中通常称为 Model ID。它不是随意显示的中文名称，而是平台识别模型的精确字符串，例如：

```yaml
spring:
  ai:
    dashscope:
      chat:
        options:
          model: qwen-plus
```

当前项目选择 `qwen-plus`，适合通用文本生成与处理。选型时应综合考虑：

- 任务类型：通用对话、代码、推理、视觉、语音或向量化；
- 输入输出模态；
- 上下文窗口与最大输出长度；
- 是否支持流式输出、工具调用和结构化输出；
- 延迟、吞吐、限流和稳定性；
- 单价、免费额度或套餐限制；
- 地域、业务空间和账号是否拥有访问权限。

模型名可能有两种形式：

- **滚动别名**：例如 `qwen-plus`，平台可能在兼容范围内更新其底层版本。
- **快照版本**：名称带具体版本日期或版本号，行为更容易固定和回归验证。

学习和快速试验可使用滚动别名；对输出稳定性要求高的生产系统，应评估快照模型，并在切换模型前执行回归测试。

以下情况通常说明模型配置有问题：

| 现象 | 优先检查 |
|---|---|
| 404 `model_not_found` | Model ID 拼写、大小写、套餐支持列表 |
| 403 无模型权限 | 业务空间授权、套餐版本、账号权限 |
| 文本模型收到图片后报 400 | 模型模态与输入内容不匹配 |
| 工具调用字段不生效 | 模型或调用协议是否支持该能力 |

## DeepSeek 模型如何接入

DeepSeek 不是一种固定接入方式。当前项目可以选择两条路径：

| 路径 | API Key 与账单 | 对当前项目的影响 | 适合场景 |
|---|---|---|---|
| 通过阿里云百炼调用 DeepSeek | 使用百炼 Key、业务空间和账单 | 继续使用现有 DashScope/Spring AI Alibaba 接入 | 希望少改代码、统一在百炼管理模型 |
| 直连 DeepSeek 官方 API | 使用 DeepSeek 官方 Key 和账单 | 新增 OpenAI 兼容客户端，并处理多个 `ChatModel` | 需要直接使用官方服务或与百炼解耦 |

两条路径的 Key、Base URL 和账户额度完全独立，不能混用。

### 当前应选择哪个模型

截至 2026-09-01，DeepSeek 官方 API 的主要模型 ID 是：

| Model ID | 定位 |
|---|---|
| `deepseek-v4-pro` | 更偏向复杂编程、数学、通用任务和高质量推理 |
| `deepseek-v4-flash` | 更偏向速度、成本和高频调用 |

旧别名 `deepseek-chat` 和 `deepseek-reasoner` 已于 2026-07-24 停用，新接入不要再使用。百炼仍列出部分 V3、R1 模型，但官方公告显示这些模型将在 2026-10-10 下架，因此不适合作为新项目的长期默认选择。

学习项目可先用 `deepseek-v4-flash` 控制成本；需要测试复杂 Agent、代码或数学任务时，再对比 `deepseek-v4-pro`。

### 路径一：通过百炼调用 DeepSeek

这是当前 `ai-agent-demo` 改动最小的方向，因为项目已经使用：

```xml
<artifactId>spring-ai-alibaba-starter-dashscope</artifactId>
```

准备工作：

1. 在百炼控制台确认业务空间已获得 DeepSeek 模型权限。
2. 获取该业务空间的 `WorkspaceId`。
3. 确认 API Key、业务空间和地域一致。
4. 将 Key 配置到 `DASHSCOPE_API_KEY`，不要写入 Git。
5. 将默认模型从 `qwen-plus` 改为 DeepSeek Model ID。

最小模型配置是：

```yaml
spring:
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        options:
          model: deepseek-v4-flash
```

需要更强能力时改为：

```yaml
model: deepseek-v4-pro
```

当前项目的大部分组件都注入同一个 `dashscopeChatModel`。修改全局 `model` 后，`LoveApp`、`YuManus`、查询改写和部分 RAG 组件都会一起切换模型，而不只是某一个接口。

百炼当前使用地域和业务空间专属地址。例如华北 2（北京）的原生 DashScope HTTP 地址是：

```text
https://{WorkspaceId}.cn-beijing.maas.aliyuncs.com/api/v1/services/aigc/text-generation/generation
```

Spring AI Alibaba Starter 会根据自身版本拼接原生 DashScope 请求路径。不要把 OpenAI 兼容地址 `/compatible-mode/v1` 直接填入 DashScope 原生客户端。若默认地址不能访问 DeepSeek，应按当前 Starter 版本配置业务空间原生 Base URL，或评估升级 Spring AI Alibaba；不要靠反复拼接路径试错。

当前项目还显式依赖了 DashScope Java SDK `2.19.1`，而百炼 DeepSeek V4 的官方 Java SDK 示例要求不低于 `2.19.4`。这条要求直接影响 `SdkAiInvoke` 一类直接使用 DashScope Java SDK 的代码。升级时至少改为：

```xml
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>dashscope-sdk-java</artifactId>
    <version>2.19.4</version>
</dependency>
```

Spring AI Alibaba Starter 本身还有独立的版本兼容关系，不能只因为 Java SDK 升级成功，就假设旧 Starter 已完整支持 DeepSeek V4 的思考内容、工具调用和新增参数。应分别验证同步对话、流式输出、Function Calling 和 reasoning 内容解析。

先用官方 HTTP 请求验证账号、Key、地域、WorkspaceId 和模型权限，可以把“平台配置问题”与“Java 框架兼容问题”分开：

```bash
curl -X POST "https://${BAILIAN_WORKSPACE_ID}.cn-beijing.maas.aliyuncs.com/api/v1/services/aigc/text-generation/generation" \
  -H "Authorization: Bearer ${DASHSCOPE_API_KEY}" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "deepseek-v4-flash",
    "input": {
      "messages": [
        {"role": "user", "content": "请用一句话介绍你自己"}
      ]
    },
    "parameters": {
      "result_format": "message"
    }
  }'
```

验证时不要把命令、终端历史或响应日志连同真实 Key 提交到仓库。

### 路径二：直连 DeepSeek 官方 API

DeepSeek 官方 API 兼容 OpenAI Chat Completions 协议。当前官方配置为：

```text
API Key 环境变量：DEEPSEEK_API_KEY
Base URL：https://api.deepseek.com
Model：deepseek-v4-pro 或 deepseek-v4-flash
```

先用 HTTP 验证凭证：

```bash
curl https://api.deepseek.com/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ${DEEPSEEK_API_KEY}" \
  -d '{
    "model": "deepseek-v4-flash",
    "messages": [
      {"role": "user", "content": "请用一句话介绍你自己"}
    ],
    "stream": false
  }'
```

在 Spring AI 中，可以通过 OpenAI 兼容 Starter 接入。在当前项目已有的 Spring AI BOM 管理下增加：

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-starter-model-openai</artifactId>
</dependency>
```

对应配置示例：

```yaml
spring:
  ai:
    openai:
      api-key: ${DEEPSEEK_API_KEY}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-v4-flash
```

该方式复用了 OpenAI 兼容协议，但服务提供方仍然是 DeepSeek，Key 也必须来自 DeepSeek，不能使用 OpenAI Key 或百炼 Key。

### 同时使用 Qwen 和 DeepSeek

如果保留 DashScope Starter，又新增 OpenAI 兼容 Starter，Spring 容器中会同时存在多个 `ChatModel`。此时仅写：

```java
public LoveApp(ChatModel chatModel) {
}
```

可能因为无法判断注入哪个 Bean 而启动失败。应该显式选择，例如：

```java
public LoveApp(@Qualifier("openAiChatModel") ChatModel deepSeekChatModel) {
    this.chatClient = ChatClient.builder(deepSeekChatModel).build();
}
```

或者继续使用百炼模型：

```java
public LoveApp(@Qualifier("dashscopeChatModel") ChatModel bailianChatModel) {
    this.chatClient = ChatClient.builder(bailianChatModel).build();
}
```

Bean 名称受当前 Spring AI/Spring AI Alibaba 版本影响，实施前应先查看自动配置实际注册的名称。更可维护的方式是为业务显式定义 `qwenChatClient`、`deepSeekChatClient`，让业务代码按用途注入，而不是在全局配置中来回切换模型。

### 接入后必须验证

不要只验证“能回答你好”。至少检查：

1. 普通同步对话能否返回文本。
2. SSE 流式输出是否连续并能正常结束。
3. 多轮消息角色和 Chat Memory 是否正常。
4. Function Calling 的工具名、参数和返回结果能否解析。
5. 思考模式的 reasoning 内容是否与最终回答分离。
6. JSON/结构化输出是否符合当前模型和协议能力。
7. 401、403、404、429 和超时是否被正确记录与转换。
8. Token 用量、延迟和费用是否符合预期。

当前项目最稳妥的接入顺序是：先用百炼官方 HTTP 请求验证 DeepSeek V4，再在独立配置或测试中验证 Spring AI，最后决定是全局替换 `qwen-plus`，还是保留两个具名 `ChatClient`。

## 理解 Base URL

Base URL 是客户端拼接具体 API 路径时使用的服务根地址，它同时决定“请求发给谁”和“使用哪种协议”。

百炼对外提供不止一种接入协议。以中国大陆版为例，OpenAI 兼容接口的官方 Base URL 是：

```text
https://dashscope.aliyuncs.com/compatible-mode/v1
```

国际版、Token Plan、Coding Plan、独立业务空间以及其他协议可能使用不同地址。API Key 必须与地域、套餐和 Base URL 配套，不能只复制一个看起来相似的地址。

特别注意：

- OpenAI 兼容 SDK 应使用对应的 `/compatible-mode/v1` 地址。
- DashScope 原生 SDK 或 Spring AI Alibaba Starter 可能使用原生端点和自己的路径拼接规则。
- 当前项目没有显式覆盖 DashScope Base URL，使用 Starter 默认值。
- 除非接入代理、私有部署或文档明确要求，否则不要随意修改当前 Starter 的 Base URL。
- 不要把 Ollama 的 `http://localhost:11434` 配置到 DashScope 下。

Base URL 常见错误包括：

- 地域地址与 Key 不匹配；
- 套餐专属 Key 调用了普通按量地址，或相反；
- 把完整接口 URL 当作 Base URL，导致路径被重复拼接；
- 少写协议、端口或路径前缀；
- 企业代理改写请求或移除了 `Authorization` 请求头。

## 理解超时

模型生成通常比普通数据库查询慢，不能直接照搬很短的 HTTP 超时。需要区分：

| 超时类型 | 含义 | 常见原因 |
|---|---|---|
| 连接超时 | 在限定时间内没有与平台建立连接 | DNS、网络、代理、TLS、地址错误 |
| 读取/响应超时 | 已连接，但在限定时间内没有收到所需响应 | 模型推理慢、输入过长、平台拥塞 |
| 流式空闲超时 | 流式连接一段时间没有收到新事件 | 首 Token 慢、中间推理、网络中断 |
| 应用总超时 | 整个业务请求超过允许时长 | 重试累积、工具链过长、下游缓慢 |

超时值应结合模型、输入长度、是否深度推理、是否流式输出和接口 SLA 设置。具体配置入口取决于 Spring AI Alibaba 版本及其底层 HTTP 客户端，不能把其他版本的属性名直接复制到本项目。

处理原则：

1. 日志记录请求 ID、模型名、耗时、异常类型和重试次数，但不记录 API Key 或完整敏感 Prompt。
2. 区分连接失败与模型响应慢，再调整对应超时。
3. 对 429、网络瞬断和部分 5xx 使用有上限的指数退避重试。
4. 对 400、401、403、模型名错误等确定性配置问题不要盲目重试。
5. 重试可能增加费用；涉及工具调用或外部写操作时，还要防止重复副作用。
6. 面向用户的长响应优先考虑 SSE 流式输出，同时配置合理的网关空闲超时。

单纯把超时无限调大只会让线程和连接占用更久。更完整的方案还包括取消请求、并发限制、熔断、降级和可观测性。

## 鉴权失败排查

### 401：未提供或无效的 API Key

按下面顺序检查：

1. 运行 Java 进程的环境中是否真的存在变量，而不是只在另一个终端中存在。
2. IDE 是否在设置变量之前已经启动。
3. Spring Profile 是否正确加载了本地配置。
4. Key 是否复制完整，前后是否有空格、引号或换行。
5. API Key 类型、地域、套餐和 Base URL 是否配套。
6. 代理或网关是否保留鉴权请求头。
7. Key 是否已被轮换、删除或失效。

安全验证变量是否存在时，只检查长度或是否为空，不打印完整值。例如应用启动时只输出：

```text
DASHSCOPE_API_KEY configured: true
```

### 403：已识别身份但无权限

常见原因包括：

- Key 所属业务空间没有模型权限；
- 使用了套餐不支持的模型；
- RAM 用户权限不足；
- IP 白名单不允许当前出口 IP；
- 账号欠费、额度或服务状态受限。

### 不要混淆的错误

| 状态或异常 | 更可能的方向 |
|---|---|
| 400 | 请求参数、输入格式、模型能力不匹配 |
| 401 | 缺少 Key、Key 无效、Key 与 Base URL 不匹配 |
| 403 | 身份有效但没有模型、空间或网络权限 |
| 404 | Base URL/路径错误或模型不存在 |
| 429 | 请求频率或额度超限 |
| 5xx | 平台或网关异常，可有限重试 |
| Connect timeout | DNS、网络、代理、地址或 TLS 问题 |
| Read timeout | 模型响应慢、请求复杂或超时配置过短 |

## 本项目核对结果

截至本笔记整理时，已确认：

- `ai-agent-demo` 使用 `spring-ai-alibaba-starter-dashscope`。
- 当前模型配置为 `qwen-plus`。
- `spring.profiles.active` 为 `local`。
- `.gitignore` 已忽略 `application-local.yml`。
- `application.yml` 已被 Git 跟踪，其中的 DashScope API Key 当前不是环境变量占位符。
- DashScope 没有显式配置 Base URL；`localhost:11434` 属于 Ollama。
- 当前配置中没有看到明确的 DashScope 超时项。

因此存在一个待处理的安全事项：确认 `application.yml` 中是否曾放入真实 Key；如果是，应先去百炼控制台轮换 Key，再将配置改为环境变量引用或移入被忽略的本地配置文件。本文档没有读取、记录或展示该 Key 的值。

## 接入检查清单

### 首次接入

- [ ] 已从模型平台创建独立 API Key。
- [ ] 已确认 Key 的地域、业务空间、套餐和模型权限。
- [ ] 已选择正确的 Model ID。
- [ ] 已确认客户端协议与 Base URL 匹配。
- [ ] Key 通过环境变量或 Secret 注入。
- [ ] 本地密钥文件已加入 `.gitignore`，且从未被 Git 跟踪。
- [ ] 已设置合理的连接、读取和应用总超时。
- [ ] 日志不会输出 Key、鉴权头或敏感 Prompt。

### 出现故障

- [ ] 先记录 HTTP 状态码、异常类型、请求 ID 和耗时。
- [ ] 401 检查 Key 注入和 Base URL 配套关系。
- [ ] 403 检查业务空间、模型权限、IP 白名单和账号状态。
- [ ] 404 检查 Base URL、请求路径和 Model ID。
- [ ] 429 区分频率限制与额度耗尽。
- [ ] 超时区分连接超时、读取超时和流式空闲超时。
- [ ] 只对可恢复错误执行有上限的退避重试。

## 自测问题

1. 为什么把 `application.yml` 加入 `.gitignore` 不能保护已经提交过的 Key？
2. API Key 泄露后，为什么必须轮换，而不只是删除代码中的值？
3. Model ID 和模型的产品展示名称有什么区别？
4. 为什么正确的 Key 配上错误的 Base URL 仍可能返回 401？
5. `localhost:11434` 在当前项目中属于哪个模型平台？
6. 连接超时和读取超时分别应该从哪些方向排查？
7. 为什么 401 不适合自动重试，而 429 或部分 5xx 可以有限重试？

## 参考资料

- [阿里云百炼：获取与配置 API Key](https://help.aliyun.com/zh/model-studio/get-api-key/)
- [阿里云百炼：调用 DeepSeek 系列模型](https://help.aliyun.com/zh/model-studio/deepseek-api)
- [阿里云百炼：常见接入错误与处理方式](https://help.aliyun.com/zh/model-studio/token-plan-personal-faq)
- [DeepSeek 官方 API：首次调用](https://api-docs.deepseek.com/quick_start/pricing-details-cny/)
- [DeepSeek 官方 API：模型更新记录](https://api-docs.deepseek.com/updates/)
- [Spring AI Alibaba](https://github.com/alibaba/spring-ai-alibaba)

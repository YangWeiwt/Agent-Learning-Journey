# 00｜大模型基础概念

这篇笔记建立后续学习 AI Agent 所需的最小概念体系。先理解模型如何接收输入、生成输出，以及应用如何维护对话状态，再学习 Spring AI、Chat Memory、Tool Calling 和 RAG，会更容易分清每一层的职责。

## 一次模型调用的基本过程

一次最简化的大模型调用可以表示为：

```text
应用组织消息和参数
        ↓
消息被转换为 Token
        ↓
模型根据当前上下文预测后续 Token
        ↓
Token 被还原为文本或结构化结果
        ↓
应用保存、展示或继续处理结果
```

大模型不是传统意义上的知识库查询器。它根据训练得到的参数和本次请求提供的上下文，逐步预测接下来更合适的 Token。

## 大模型

大语言模型（Large Language Model，LLM）是在大量数据上训练得到的神经网络模型。它的核心能力是：给定一段上下文，计算后续 Token 的概率分布，并据此生成内容。

从应用开发角度，可以把大模型看成一个具有以下特征的生成组件：

- 输入可以包含指令、用户问题、历史消息、检索资料和工具定义。
- 输出可以是自然语言、结构化数据或工具调用请求。
- 输出具有概率性，相同输入不保证每次逐字一致。
- 模型可能生成看似合理但事实错误的内容，因此关键结论需要验证。
- 模型的训练知识、请求上下文和外部工具是三种不同的信息来源。

需要区分“模型能力”和“应用能力”：联网搜索、数据库查询、长期记忆、权限校验等通常由模型外部的应用或工具提供，不是模型天然拥有的能力。

## Token

Token 是模型处理输入和生成输出时使用的基本单位。文本会先经过分词器编码为 Token ID，模型处理的是这些 ID，而不是直接处理 Java 字符串或汉字。

Token 不等于“一个字”或“一个单词”：

- 一个英文单词可能被拆成一个或多个 Token。
- 一个汉字、标点或空格也会占用 Token，但具体数量取决于模型使用的分词器。
- 代码、JSON、长数字和生僻文本的 Token 数量可能高于直觉估计。
- 不同模型或分词器对同一段文本的切分结果可能不同。

Token 主要影响三个方面：

1. **容量**：输入和输出必须满足模型的上下文限制。
2. **费用**：API 通常分别统计输入 Token 和输出 Token。
3. **延迟**：输入越长、输出越多，通常需要更多处理时间。

一次请求的用量通常可以抽象为：

```text
total_tokens = input_tokens + output_tokens
```

部分推理模型还会在 `output_tokens` 中记录或细分 reasoning tokens。不能仅用最终可见文字的长度反推完整用量。

## Prompt

Prompt 是提供给模型、用于引导本次生成的输入集合，不只是用户输入的那一句话。一个完整 Prompt 可能包含：

- 系统或开发者指令；
- 当前用户消息；
- 之前的用户和助手消息；
- RAG 检索出的资料；
- 工具名称、说明和参数结构；
- 输出格式、示例和约束。

例如：

```text
开发者指令：你是一个 Java 学习助手，回答时给出可运行示例。
历史消息：用户之前正在学习 Spring AI。
用户消息：解释 ChatClient 和 ChatModel 的区别。
输出约束：先给结论，再给代码示例。
```

好的 Prompt 通常会明确：任务目标、必要背景、约束条件、输出格式和判断标准。Prompt 能提高结果质量，但不能保证模型掌握不存在于上下文或训练数据中的事实。

## 上下文窗口

上下文窗口（context window）是模型单次生成时能够处理的 Token 容量上限。它不是“最多能保存多少条聊天记录”，而是本次调用中模型实际可见内容的总预算。

通常会占用上下文窗口的内容包括：

- system/developer 指令；
- 用户和助手消息；
- 工具定义与工具结果；
- 检索资料、图片或文件对应的模型输入；
- 为本次回答预留的输出空间。

可用容量可以粗略理解为：

```text
模型上下文上限 ≈ 输入 Token + 本次可生成的 Token
```

不同 API 和模型对超限的处理不同，常见策略包括：

- 请求失败并提示超过上下文限制；
- 从较早的消息开始截断；
- 先对较早内容做摘要或压缩，再把摘要放回上下文。

因此，“对话记录仍然存在于数据库”不等于“模型本轮能够看到全部记录”。应用必须决定选取、裁剪、总结或检索哪些历史内容。

## 消息角色与指令优先级

聊天式 API 使用角色标记消息来源和用途。常见角色如下：

| 角色 | 主要来源 | 主要用途 |
|---|---|---|
| `system` | 平台或最上层运行环境 | 定义最高层行为、边界和全局约束 |
| `developer` | 应用开发者 | 定义产品角色、任务规则、输出要求和工作流程 |
| `user` | 最终用户 | 表达本轮目标、问题和提供的数据 |
| `assistant` | 模型 | 表示模型先前生成的回复，也可作为后续上下文 |

在 OpenAI 的消息层级中，可以先用下面的简化顺序理解：

```text
system > developer > user > assistant
```

高优先级指令和低优先级请求冲突时，应遵循高优先级指令。例如，developer 消息要求“不得输出密钥”，user 消息要求“打印密钥”，模型应拒绝后者。

几个容易混淆的点：

- `system` 和 `developer` 都不是普通聊天内容，而是应用控制模型行为的指令层。
- 用户通常只能直接控制 `user` 消息，应用在请求发送前加入其他消息。
- 历史 `assistant` 消息用于告诉模型之前回答过什么，不代表模型在请求之外自行保留了这段经历。
- 具体 API、SDK 或模型支持的角色可能不同，应以所用接口文档为准。

## 模型并不自动记得历史

从应用开发视角看，模型调用应默认按“无状态”理解：本次请求没有提供的信息，模型在下一次独立请求中就不可见。

假设第一次调用是：

```json
[
  {"role": "user", "content": "我叫小明"}
]
```

模型回答“你好，小明”。第二次如果只发送：

```json
[
  {"role": "user", "content": "我叫什么？"}
]
```

模型没有可靠依据回答“小明”。要实现多轮对话，应用或 API 服务必须把状态重新带入，例如：

```json
[
  {"role": "user", "content": "我叫小明"},
  {"role": "assistant", "content": "你好，小明"},
  {"role": "user", "content": "我叫什么？"}
]
```

常见状态管理方式有：

1. 应用从内存或数据库读取历史消息，并在每次请求时重新发送。
2. API 保存 Conversation/Response 状态，应用通过会话 ID 或上一响应 ID 关联后续请求。
3. 应用将早期消息总结为摘要，只发送摘要和最近几轮消息。
4. 应用把长期信息存入数据库或向量库，需要时检索并加入 Prompt。

即使 API 提供了有状态会话，也应理解为“服务端替应用保存并重新组织上下文”，而不是基础模型获得了像人一样永久、无限的记忆。

在本项目的 `ai-agent-demo` 中，`MessageChatMemoryAdvisor` 根据 `chatId` 读取历史消息，再把它们加入新的模型请求。真正维护历史的是应用层的 Chat Memory，而不是模型本身。

## temperature

`temperature` 是控制采样随机程度的参数。直观理解是：

- 较低值通常让输出更集中、更稳定，适合信息抽取、分类、代码修改等任务。
- 较高值通常增加表达变化和随机性，适合头脑风暴、创意文案等任务。

它不是“正确率”或“创造力”的直接开关。还要注意：

- 低 temperature 不能保证结果完全一致或事实一定正确。
- 高 temperature 不代表内容质量一定更高。
- 参数范围、默认值和是否支持由具体模型与 API 决定。
- `top_p` 是另一种采样控制方式，通常调整 `temperature` 或 `top_p` 其中一个即可。

工程上应通过测试集和评估结果选参数，不要只凭感觉设置。

## 最大输出 Token

`max_output_tokens`（某些接口可能使用其他相近名称）用于限制单次响应最多生成多少 Token。它的主要作用是：

- 防止回答无限延长；
- 控制延迟和费用；
- 为上下文窗口分配输入与输出预算。

达到上限时，回答可能以 `incomplete` 等状态结束，内容也可能在句子中间被截断。上限过小会导致答案不完整，上限过大则只是放宽边界，并不要求模型一定生成那么多内容。

在 OpenAI Responses API 中，`max_output_tokens` 是输出 Token 的上界，并包含可见输出 Token 和 reasoning tokens。因此，推理模型即使最终可见文本很短，也可能消耗较多输出预算。

## 参数之间的关系

可以用下面的方式理解几个核心参数：

| 配置 | 控制的问题 | 不负责什么 |
|---|---|---|
| 模型 | 使用哪套能力、知识和限制 | 不自动提供业务数据或长期记忆 |
| Prompt | 本轮让模型做什么、依据什么做 | 不扩大模型的上下文上限 |
| 上下文窗口 | 本轮最多能让模型看到和生成多少 Token | 不保证窗口内信息都被正确使用 |
| `temperature` | 如何从候选 Token 中采样 | 不保证事实正确或绝对可复现 |
| `max_output_tokens` | 本轮最多生成多少 Token | 不规定实际必须生成的长度 |

## 与当前项目的对应关系

在 `01-普通模型调用.md` 的调用链中，这些概念对应为：

| 基础概念 | 项目中的体现 |
|---|---|
| 大模型 | 可按请求选择 DeepSeek `deepseek-v4-flash` 或 MiniMax `MiniMax-M2.7` |
| Prompt | `defaultSystem(...)`、`.user(message)` 和 Advisor 加入的历史消息 |
| 消息角色 | 系统提示词、用户输入和 `AssistantMessage` |
| 对话记忆 | `MessageWindowChatMemory` 与 `MessageChatMemoryAdvisor` |
| 上下文窗口 | 系统提示词、历史消息、本轮问题及输出共同占用的模型容量 |
| 生成参数 | `ChatModel` 或 `ChatClient` 调用时使用的模型选项 |

## 常见误区

| 误区 | 正确认识 |
|---|---|
| 一个 Token 就是一个汉字 | Token 的切分由具体分词器决定 |
| Prompt 就是用户输入框里的文字 | Prompt 还可能包含指令、历史、资料和工具定义 |
| 聊天窗口里有历史，模型就永久记得 | 应用或 API 必须保存并在后续请求中提供相关状态 |
| 上下文窗口等于最大输入长度 | 通常还要考虑输出以及工具等内容所占预算 |
| temperature 设为 0 就绝对可复现 | 低值更稳定，但不等于严格确定性 |
| 最大输出设得越大，回答就越好 | 它只是上限；质量主要取决于模型、上下文和任务设计 |

## 自测问题

1. 为什么同一句中文在不同模型中可能消耗不同数量的 Token？
2. 除了用户问题，还有哪些内容会占用上下文窗口？
3. `developer` 消息与 `user` 消息冲突时，模型应优先遵循哪一个？
4. 为什么数据库中保存了完整聊天记录，仍可能出现模型“忘记”早期内容？
5. 低 temperature 为什么不能等同于事实正确？
6. `max_output_tokens` 设置过小会出现什么现象？
7. `MessageChatMemoryAdvisor` 提供的是模型能力还是应用能力？

## 参考资料

- [OpenAI Responses API：创建模型响应](https://developers.openai.com/api/reference/cli/resources/responses/methods/create)
- [OpenAI Models：模型能力、上下文窗口与最大输出](https://developers.openai.com/api/docs/models)
